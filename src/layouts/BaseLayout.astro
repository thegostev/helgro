---
// Fundations
import "@/styles/global.css";
import BaseHead from "@/components/fundations/head/BaseHead.astro";
// Components
import Search from "@/components/global/Search.astro";
import Footer from "@/components/global/Footer.astro";
import Navigation from "@/components/global/Navigation.astro";
// Hides nav and footer on selected screens by passing
// the hideNav and hideFooter props
// <BaseLayout hideNav={true} hideFooter={true}>
interface Props {
  hideNav?: boolean;
  hideFooter?: boolean;
  title?: string;
  description?: string;
  canonical?: string;
}
const { hideNav = false, hideFooter = false, title, description, canonical } = Astro.props;
---

<html lang="en" class="scroll-smooth">
  <head>
    <BaseHead title={title} description={description} canonical={canonical} />
  </head>
  <body class="flex flex-col bg-base-100 min-h-svh">
    {!hideNav && <Navigation />}
    <main class="grow"><slot /></main>
    {!hideFooter && <Footer />}
    <Search />
  </body>
</html>
